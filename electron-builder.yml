productName: "HelloVite"
appId: "io.vite.electron.template"

asar: true
# compression: maximum

directories:
  app: ./build
  buildResources: "resources"
  output: "dist"

win:
  target:
    - "zip"
    # - "nsis"

linux:
  target:
    - "zip"
    - "deb"

nsis:
  oneClick: true
  perMachine: true
